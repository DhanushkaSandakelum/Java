package shapes;

/**
 * Interface: Abstract class that used to group related methods with empty bodies
 * @desc: Any class that implements this interface must provide concrete implementations for these methods
 */
public interface Shape {
    double calculateArea();

    double calculatePerimeter();
}
